<%
  page_title = "Fornecedores"
  page_path = [
      { name: 'Inicio', link: root_path },
      { name: 'Fornecedores', link: root_path },
  ]
  button = {
      link: new_supplier_path,
      class: 'btn btn-info m-l-15',
      content: '<i class="fa fa-plus-circle"></i> Criar'.html_safe
  }
%>
<%= page_header_helper(page_title, page_path, button) %>

<div class="row">
  <div class="col-12">
    <div class="card">
      <div class="card-body">
        <div class="table-responsive">
          <table class="table">
            <thead>
            <tr>
              <th>Nome</th>
              <th colspan="3"></th>
            </tr>
            </thead>
            <tbody>
            <% if @suppliers.length > 0 %>
              <% @suppliers.each do |supplier| %>
                <tr>
                  <td><%= supplier.name %></td>
                  <td width="10">
                    <%= link_to edit_supplier_path(supplier), class: 'btn btn-sm btn-outline-info',
                                'data-toggle': "tooltip", 'data-placement':"bottom", title: "Editar" do %>
                      <i class="fa fa-edit"></i>
                    <% end %>
                  </td>
                  <td width="10">
                    <%= link_to supplier, class: "btn btn-sm btn-danger",
                                method: :delete, data: { confirm: 'Deseja excluir esse fornecedor?' },
                                'data-toggle': "tooltip", 'data-placement':"bottom", title: "Excluir" do %>
                      <i class="fa fa-trash"></i>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            <% else %>
              <tr>
                <td colspan="10">
                  <h6 class="card-subtitle card-subtitle-no-result">
                    Nenhuma subcategoria foi encontrada.
                  </h6>
                </td>
              </tr>
            <% end %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>


